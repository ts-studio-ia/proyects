# MCP Runtime Bridge

Toda capacidad externa se modela como MCP Tool + MCP UI Action + receipt auditable + replay metadata.

## Invariantes
- no bypass policy runtime
- no bypass constitutional runtime
- no secret-by-value
- execution gateway obligatorio
