{
  "name": "repo-navigator-canvas",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test dist/tests/*.test.js",
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "zustand": "^5.0.8"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
