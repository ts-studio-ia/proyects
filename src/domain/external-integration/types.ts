export type InfrastructureProvider = { id: string; name: string; type: string; enabled: boolean; readonlyByDefault: boolean };
export type InfrastructureConnector = { id: string; providerId: string; tenantId: string; projectId: string; enabled: boolean };
export type DeploymentTarget = { environment: "local" | "preview" | "staging" | "production"; previewReady: boolean; kyvernoReady: boolean; tailscaleReady: boolean };
export type RuntimeSandbox = { filesystemIsolation: boolean; networkIsolation: boolean; processIsolation: boolean; resourceQuotas: boolean; timeoutMs: number; tenantBoundary: boolean; secretBoundary: boolean; traceBoundary: boolean; mcpBoundary: boolean };
export type SandboxExecutionSession = { id: string; sandboxId: string; dryRun: boolean; started: boolean; completed: boolean };
export type ExternalCredentialReference = { provider: string; referenceId: string };
export type SecretVaultReference = { provider: "hashicorp-vault" | "doppler"; path: string; key: string };
export type VaultSecretReference = { provider: "hashicorp-vault"; path: string; key: string };
export type ExternalAuditReceipt = { id: string; operationId: string; tenantId: string; projectId: string; replayMetadata: string };
export type ExternalTraceBridge = { traceId: string; providerId: string; bridged: boolean };
export type InfrastructureHealthState = { providerId: string; healthy: boolean; message: string };
export type RealDeploymentSession = { id: string; target: DeploymentTarget; approvalApproved: boolean };
export type InfrastructureRollback = { operationId: string; rollbackReady: boolean; checkpointId: string };
export type ExternalPolicyBoundary = { policyDecisionRequired: boolean; constitutionalDecisionRequired: boolean; tenantProjectScopeRequired: boolean };
export type ExternalCapabilityPermit = { capabilityId: string; granted: boolean; reason: string };
export type RealExecutionReceipt = { id: string; operationId: string; status: "blocked" | "sandboxed" | "completed"; replayable: boolean };
export type IntegrationTrustProfile = { providerId: string; score: number; kyvernoReady: boolean; tailscaleReady: boolean; vaultReady: boolean; replayFidelity: number; auditability: number };
export type InfrastructureIsolationBoundary = { tenantIsolated: boolean; networkIsolated: boolean; secretIsolated: boolean; traceIsolated: boolean };
export type TerraformPlanPreview = { id: string; target: string; diffSummary: string; sandboxed: boolean };
export type TerraformApplyRequest = { approvalPackageApproved: boolean; policyDecisionAllow: boolean; constitutionalValidationPassed: boolean; rollbackPlanExists: boolean; blastRadiusWithinThreshold: boolean; environment: "local" | "preview" | "staging" | "production"; promotionExplicit: boolean; traceReceiptEmitted: boolean; ledgerEntryRecorded: boolean; forceDestroy: boolean };
export type KyvernoPolicyCheck = { ready: boolean; rulesValidated: string[] };
export type TailscaleMeshSession = { id: string; privateAdminAccess: boolean; workerToControlPlane: boolean; emergencyOperatorAccess: boolean; observabilityAccess: boolean; zeroTrustRouting: boolean };
export type MilvusConnectionProfile = { tenantId: string; projectId: string; provider: "milvus"; mutationRequiresApproval: boolean };
export type DrizzleConnectionProfile = { provider: "drizzle"; prismaBlocked: true; tenantBoundaryValidated: boolean; appendOnlyLedgerCompatible: boolean };
export type MCPRuntimeBridge = { id: string; policyBound: boolean; constitutionalBound: boolean };
export type MCPExternalConnector = { id: string; provider: string; enabled: boolean; requiresAuditReceipt: boolean; replayable: boolean };
export type MCPToolExecutionGateway = { id: string; policyRequired: boolean; constitutionalRequired: boolean; secretByReferenceOnly: boolean };
export type MCPUIRuntimeBinding = { actionId: string; gatewayId: string; governanceBound: boolean };
